<svg viewBox="0, 0, 100, 100" ng-attr-width = "{{widthCell}}" ng-attr-height= "{{widthCell}}">
	<circle ng-attr-cx="{{widthCell}}" ng-attr-cy="{{widthCell}}" ng-attr-r= "{{widthCell}}" ng-attr-fill="{{item.color}}" stroke="#4C4C4C" stroke-width="2"/>
</svg>
